project(w3m)
cmake_minimum_required(VERSION 3.0.0)
set(CMAKE_CXX_STANDARD 20)
set(W3M_PREFIX $ENV{HOME}/local)
   
add_library(catch2 INTERFACE)
target_include_directories(catch2
INTERFACE
    _external/catch2
)

subdirs(
    charutil
    libwc
)

add_executable(w3mbookmark
    src/w3mbookmark.cpp
    src/indep.cpp
    src/regex.cpp
    src/textlist.cpp
    src/html/parsetag.cpp
    src/hash.cpp
    )
target_include_directories(w3mbookmark
PRIVATE
    ${CMAKE_CURRENT_LIST_DIR}/src
    )  
target_compile_definitions(w3mbookmark
PUBLIC
    DUMMY
    HAVE_CONFIG_H
    AUXBIN_DIR=\"${W3M_PREFIX}/libexec/w3m\"
    CGIBIN_DIR=\"${W3M_PREFIX}/libexec/w3m/cgi-bin\"
    HELP_DIR=\"${W3M_PREFIX}/share/w3m\"
    ETC_DIR=\"${W3M_PREFIX}/etc\"
    CONF_DIR=\"${W3M_PREFIX}/etc/w3m\"
    RC_DIR=\"~/.w3m\"
    LOCALEDIR=\"${W3M_PREFIX}/share/locale\"
    USE_UNICODE
    )
target_link_libraries(w3mbookmark
PRIVATE
    wc
    )
        
add_executable(w3m
    src/main.cpp
    src/w3m.cpp
    src/cli.cpp
    #
    src/dispatcher.cpp
    src/commands.cpp
    src/public.cpp
    src/urimethod.cpp
    src/frontend/display.cpp
    src/frontend/link.cpp
    src/frontend/lineinput.cpp
    src/frontend/line.cpp
    src/frontend/buffer.cpp
    src/frontend/tab.cpp
    src/frontend/tabbar.cpp
    src/frontend/search.cpp
    src/frontend/menu.cpp
    src/frontend/terms.cpp
    src/frontend/mouse.cpp
    src/frontend/keybind.cpp
    src/frontend/event.cpp
    src/file.cpp
    src/transport/istream.cpp
    src/transport/loader.cpp
    src/transport/local.cpp
    src/transport/urlfile.cpp
    src/transport/url.cpp
    src/mime/mimeencoding.cpp
    src/mime/mimetypes.cpp
    src/mime/mailcap.cpp
    src/http/compression.cpp
    src/http/auth.cpp
    src/http/http_request.cpp
    src/http/cookie.cpp
    src/http/ftp.cpp
    src/http/news.cpp
    src/html/anchor.cpp
    src/html/tagstack.cpp
    src/html/html_processor.cpp
    src/html/table.cpp
    src/html/form.cpp
    src/html/parsetag.cpp
    src/html/html.cpp
    src/html/frame.cpp
    src/html/maparea.cpp
    src/html/image.cpp
    src/backend.cpp
    src/mytime.cpp
    src/indep.cpp
    src/rc.cpp
    src/regex.cpp    
    src/textlist.cpp
    src/hash.cpp
    src/symbol.cpp
    src/history.cpp
)
target_include_directories(w3m
PRIVATE
    ${CMAKE_CURRENT_LIST_DIR}/src
    )
target_compile_definitions(w3m
PUBLIC
    HAVE_CONFIG_H
    AUXBIN_DIR=\"${W3M_PREFIX}/libexec/w3m\"
    CGIBIN_DIR=\"${W3M_PREFIX}/libexec/w3m/cgi-bin\"
    HELP_DIR=\"${W3M_PREFIX}/share/w3m\"
    ETC_DIR=\"${W3M_PREFIX}/etc\"
    CONF_DIR=\"${W3M_PREFIX}/etc/w3m\"
    RC_DIR=\"~/.w3m\"
    LOCALEDIR=\"${W3M_PREFIX}/share/locale\"
    USE_UNICODE
    )
target_compile_options(w3m
PRIVATE
    # -Werror=implicit-function-declaration
    # -Werror=int-conversion
    -Werror=conversion-null
    )
target_link_libraries(w3m
PRIVATE
    wc
    m
    ncurses
    ssl
    crypto
    )
